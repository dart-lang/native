{
    "$schema": "https://json-schema.org/draft-07/schema#",
    "title": "Link input schema for SDKs.",
    "type": "object",
    "allOf": [
        {
            "$ref": "link_input_shared.schema.json#"
        }
    ],
    "required": [
        "version",
        "build_asset_types",
        "package_name",
        "package_root",
        "out_file",
        "out_dir",
        "out_dir_shared"
    ],
    "_versionSkew": {
        "out_file": "'out_file' is not read by older hooks. If the file doesn't exist, then it must be $out_dir/output.json.",
        "version": "Older hooks will read 'version', so it must be provided."
    }
}
