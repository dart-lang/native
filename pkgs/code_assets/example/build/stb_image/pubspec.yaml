name: stb_image
description: An example for `package:code_assets` demonstrating how to bundle a locally built stb_image dynamic library.
version: 0.1.0
repository: https://github.com/dart-lang/native/tree/main/pkgs/code_assets/example/build/stb_image

publish_to: none

resolution: workspace

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  code_assets: any
  ffi: ^2.1.4
  hooks: any
  logging: ^1.3.0
  native_toolchain_c: any

dev_dependencies:
  ffigen: ^19.1.0
  lints: ^6.0.0
  test: ^1.25.15
