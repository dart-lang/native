# android_libcpp_shared_example

Example showing the use of the `android_libcpp_shared` package to include the shared Android C++ runtime in a Flutter app.

This just creates a random number using a native function call.