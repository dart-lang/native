name: system_library_example
description: Uses some functions from system lirbaries.
version: 0.1.0

publish_to: none

resolution: workspace

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  code_assets: any
  hooks: any
  logging: ^1.3.0
  native_toolchain_c: any

dev_dependencies:
  ffigen: ^20.0.0-dev.1
  lints: ^6.0.0
  test: ^1.25.15
