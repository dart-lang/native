name: app_with_asset_treeshaking
description: A sample command-line application.
version: 0.1.0

publish_to: none

resolution: workspace

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  logging: ^1.3.0
  package_with_assets:
    path: ../package_with_assets/

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
