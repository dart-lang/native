name: package_with_assets
description: An example of using a package with assets.
version: 0.1.0

publish_to: none

resolution: workspace

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  data_assets: any
  hooks: any
  logging: ^1.3.0
  meta: ^1.16.0
  record_use: any

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.15
