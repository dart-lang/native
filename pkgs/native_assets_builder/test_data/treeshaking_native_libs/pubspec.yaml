name: treeshaking_native_libs
description: Treeshake symbols by name from a native library.
version: 0.1.0

publish_to: none

resolution: workspace

environment:
  sdk: '>=3.7.0 <4.0.0'

dependencies:
  logging: ^1.3.0
  native_assets_cli: ^0.17.0-wip
  native_toolchain_c: ^0.14.0-wip

dev_dependencies:
  ffigen: ^18.0.0
  lints: ^5.1.1
  some_dev_dep:
    path: ../some_dev_dep/
  test: ^1.25.15
