name: transformer
description: Transforms assets, caches the transformation, and caches across build invocations.
version: 0.1.0

publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  crypto: ^3.0.6
  # native_assets_cli: ^0.10.0
  native_assets_cli: ^0.11.0-wip

resolution: workspace
