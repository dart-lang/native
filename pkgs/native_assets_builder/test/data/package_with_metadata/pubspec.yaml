name: package_with_metadata
description: Sets some metadata in its build.dart
version: 0.1.0

publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  cli_config: ^0.1.1
  native_assets_cli: ^0.2.0
  yaml: ^3.1.1
  yaml_edit: ^2.1.0

dev_dependencies:
  lints: ^2.0.0
