name: dart_app

publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  native_add:
    path: ../native_add
  native_subtract:
    path: ../native_subtract

dependency_overrides:
  native_assets_cli:
    path: ../../../../native_assets_cli/
