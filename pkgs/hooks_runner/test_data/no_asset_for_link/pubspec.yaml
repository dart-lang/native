name: no_asset_for_link
description: A link hook, but no assets for link.
version: 0.1.0

publish_to: none

resolution: workspace

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  code_assets: ^0.19.2
  data_assets: ^0.19.1
  hooks: ^0.19.2
  logging: ^1.3.0
  meta: ^1.16.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
