name: simple_link
description: Build 4 assets for linking, only keep two in link.dart.
version: 0.1.0

publish_to: none

resolution: workspace

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  cli_config: ^0.2.0
  data_assets: any
  hooks: any
  logging: ^1.3.0

dev_dependencies:
  lints: ^6.0.0
  path: ^1.9.0
  test: ^1.25.15
