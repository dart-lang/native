name: infra_failure
description: Always fails with an infra failure
version: 0.1.0

publish_to: none

resolution: workspace

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  code_assets: ^0.19.0
  hooks: ^0.19.0

dev_dependencies:
  ffigen: ^20.0.0-dev.1
  lints: ^6.0.0
  test: ^1.25.15
