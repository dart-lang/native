name: depend_on_fail_build
description: Has a dependency that fails to build.
version: 0.1.0

publish_to: none

resolution: workspace

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  fail_build:
    path: ../fail_build/
  hooks: any

dev_dependencies:
  ffigen: ^19.1.0
  lints: ^6.0.0
  test: ^1.25.15
