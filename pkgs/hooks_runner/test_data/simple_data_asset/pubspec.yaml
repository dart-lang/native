name: simple_data_asset
description: Add and retrieve a data asset.
version: 0.1.0

publish_to: none

resolution: workspace

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  data_assets: ^0.19.1
  hooks: ^0.19.1
  logging: ^1.3.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
