name: use_all_api
description: Example to use all API.

version: 0.1.0

publish_to: none

resolution: workspace

environment:
  sdk: '>=3.7.0 <4.0.0'

dependencies:
  cli_config: ^0.2.0
  code_assets: ^0.19.0-wip
  data_assets: ^0.19.0-wip
  hooks: ^0.19.0-wip
  logging: ^1.3.0

dev_dependencies:
  lints: ^5.1.1
  path: ^1.9.0
  test: ^1.25.15
