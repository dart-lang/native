import { createRoot, createSignal } from "solid-js";

export interface LogEntry {
  level: number;
  message: string;
}

/**
 * Array of log entries generated by ffigen
 */
export const $logs = createRoot(() => createSignal<LogEntry[]>([]));
