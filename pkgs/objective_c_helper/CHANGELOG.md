## [Unreleased]

### Added
- New helper package `objective_c_helper` containing:
  - `lib/src/util.c` (test C utilities)
  - `lib/src/util.dart` (Dart wrappers for testing)
- Build hook that skips native compilation on non-macOS platforms.
