name: example
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  kotlin_plugin:
    path: ../
  
  jni:
    path: ../../../../jni

dependency_overrides:
  jni_util:
    path: ../../../../jni_util

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

