name: pdf_info
description: Dart standalone example using jnigen PDFBox bindings.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  path: ^1.8.0
  jni:
    path: ../../../../jni/
  pdfbox_plugin:
    path: ../

dev_dependencies:
  test: ^1.16.0
