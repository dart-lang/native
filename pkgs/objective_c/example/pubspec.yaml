name: example
description: "Demonstrates using package:objective_c"
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.9.0-21.0.dev <4.0.0'

dependencies:
  objective_c:
    path: ..

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
