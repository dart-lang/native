name: use_dart_api
description: Uses some functions from `dart_api_dl.h`.
version: 0.1.0

publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace
dependencies:
  logging: ^1.1.1
  native_assets_cli: ^0.11.0-wip
  native_toolchain_c: ^0.8.0-wip

dev_dependencies:
  ffigen: ^16.0.0
  test: ^1.23.1
