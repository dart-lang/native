publish_to: none

name: local_asset
description: Uses an asset local to the package.
version: 0.1.0
repository: https://github.com/dart-lang/native/tree/main/pkgs/native_assets_cli/example/build/native_add_library

resolution: workspace

environment:
  sdk: '>=3.7.0 <4.0.0'

dependencies:
  logging: ^1.3.0
  native_assets_cli: ^0.17.0-wip

dev_dependencies:
  ffigen: ^18.0.0
  lints: ^5.1.1
  test: ^1.25.15
